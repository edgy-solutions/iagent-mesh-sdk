[project]
name = "REPLACE_ME_NAME"
version = "0.1.0"
description = "An autonomous agent node for the iagent Mesh"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "iagent-mesh",
    "dag-tools",
    "smolagents",
    "nest_asyncio"
]
